apiVersion: 1
providers:
  - name: voyager-obs
    orgId: 1
    folder: Voyager
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
